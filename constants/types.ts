export interface WebAppRequestBody {
    name: string;
    lastName: string;
    phone: string;
    country: string;
    region: string;
    districts: string;
    query_id: string;
    user_id: number;
}